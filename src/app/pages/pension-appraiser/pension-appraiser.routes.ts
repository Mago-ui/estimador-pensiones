import { Routes } from '@angular/router';
import { PensionAppraiserComponent } from './pension-appraiser';

export const WELCOME_ROUTES: Routes = [
  { path: '', component: PensionAppraiserComponent },
];
